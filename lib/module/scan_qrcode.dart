import 'package:flutter/material.dart';

class ScanQrCodePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('ScanQrCode'), backgroundColor: Colors.green),
      body: Container(
        color: Colors.blue,
      ),
    );
  }
}
